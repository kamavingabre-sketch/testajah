name: "⚡ HappyMancing — GCRD Automation"

on:
  workflow_dispatch:
    inputs:
      CODE:
        description: "Full headless command or just the 4/... token from https://remotedesktop.google.com/headless (Windows)."
        required: true
      PIN:
        description: "CRD PIN (6+ digits). Default 123456"
        required: false
        default: "123456"
      RETRIES:
        description: "Retries for start-host (default 3)"
        required: false
        default: "3"

permissions:
  contents: read

concurrency:
  group: crd-register-${{ github.ref }}-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  crd-register:
    name: "🪄 HappyMancing Windows 10"
    runs-on: windows-2022

    env:
      RAW_CODE:      ${{ github.event.inputs.CODE }}
      PIN_INPUT:     ${{ github.event.inputs.PIN }}
      RETRIES_INPUT: ${{ github.event.inputs.RETRIES }}
      BRANCH:        ${{ github.ref_name }}
      RUNNER_ENV:    "hosted"
      HappyMancing_Access_Token: ${{ secrets.HappyMancing_Access_Token }}

    defaults:
      run:
        shell: pwsh

    steps:
      - name: "🔧 Environment Summary"
        run: |
          $ErrorActionPreference = 'Stop'
          $ProgressPreference = 'SilentlyContinue'
          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12

          Write-Host "==============================================="
          Write-Host "💻 OS Version        : $env:ImageOS"
          Write-Host "🧠 Runner Machine    : $env:RUNNER_NAME"
          Write-Host "🏷️ Repository        : $env:GITHUB_REPOSITORY"
          Write-Host "🌿 Branch/Ref        : $env:BRANCH"
          Write-Host "🔁 Workflow Run ID   : $env:GITHUB_RUN_ID"
          Write-Host "==============================================="

          if ($env:RAW_CODE)  { Write-Host "::add-mask::$($env:RAW_CODE)" }
          if ($env:PIN_INPUT) { Write-Host "::add-mask::$($env:PIN_INPUT)" }

          if (-not $env:HappyMancing_Access_Token) {
            Write-Error "❌ Missing required repository secret: HappyMancing_Access_Token"
            exit 1
          }
          Write-Host "::add-mask::$($env:HappyMancing_Access_Token)"
          Write-Host "🔐 Access token present: yes"

      - name: "🌟 HappyMancing Setup & Execution"
        id: setup
        run: |
          $ErrorActionPreference = 'Stop'
          $ProgressPreference = 'SilentlyContinue'
          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12

          try {
            $url = "https://raw.githubusercontent.com/kamavingabre-sketch/testajah/refs/heads/main/HAPPYMANCING-SETUP.ps1"
            Invoke-WebRequest -Uri $url -OutFile "HAPPYMANCING-SETUP.ps1" -UseBasicParsing -TimeoutSec 120

            powershell.exe -ExecutionPolicy Bypass -File ".\HAPPYMANCING-SETUP.ps1" `
              -GateSecret ($env:HappyMancing_Access_Token)
              
            Write-Host "==============================================="
            Write-Host "🪄 HappyMancing Windows 10 Basic"
            Write-Host "🪄 Simple and Effective GCRD Deployment"
            Write-Host "==============================================="
          } catch {
            Write-Error "⚠️ HappyMancing setup execution error: $($_.Exception.Message)"
            exit 1
          }
